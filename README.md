# DDS 综合信号发生系统（DDS Comprehensive Signal Generator）

> 基于 **Cyclone IV FPGA + MSPM0G3519 MCU** 的直接数字频率合成（DDS）项目，支持正弦波、AM、FM、ASK、PSK 以及李萨如图测试。  
> A **Cyclone IV FPGA + MSPM0G3519 MCU** based DDS project supporting Sine/AM/FM, ASK/PSK digital modulation, and Lissajous test mode.

---

## 1. 项目概览 | Project Overview

### 中文
- 本仓库包含两个核心工程：
  - `DDS_FPGA/`：Quartus FPGA 工程（波形生成、调制、数码管显示、UART 解析）。
  - `DDS_MCU/`：Keil MSPM0 工程（键盘输入、OLED UI、参数发送、外设驱动）。
- 系统通过 MCU 键盘/OLED 完成人机交互，再通过 UART 将模式与参数发送给 FPGA，由 FPGA 实时合成并输出 DAC 数字波形。

### English
- This repository contains two main projects:
  - `DDS_FPGA/`: Quartus FPGA design (wave generation, modulation, seven-segment display, UART parsing).
  - `DDS_MCU/`: Keil MSPM0 firmware (keypad input, OLED UI, parameter transmission, peripheral drivers).
- The MCU handles user interaction and sends mode/parameters over UART to the FPGA, which synthesizes real-time DAC waveform data.

---

## 2. 目录结构 | Repository Structure

```text
DDS/
├─ DDS_FPGA/
│  ├─ DDS_simple.v            # FPGA 顶层（DDS + AM/FM + ASK/PSK + 李萨如模式）
│  ├─ MCU_uart.v              # UART 接收与协议解析（A/B/C/D + 数值）
│  ├─ dds_freq_ctrlword.v     # 频率控制字计算
│  ├─ seg_display.v           # 6 位数码管动态扫描显示
│  ├─ sine_14bit_256.mif/.v   # 14-bit 正弦查找表
│  ├─ sintopwm.v              # 正弦转 PWM（李萨如模式第二通道）
│  ├─ dac904_pll.qip/.v       # DAC 时钟 PLL
│  └─ DDS_simple.qpf/.qsf     # Quartus 工程与引脚约束
└─ DDS_MCU/
   ├─ User/main.c             # 主流程与状态机（A/B/C/D 模式 UI）
   ├─ User/config.syscfg      # SysConfig 配置源
   ├─ User/ti_msp_dl_config.* # SysConfig 生成的 DriverLib 配置
   ├─ BSP/Keyboard.*          # 4x4 键盘扫描
   ├─ BSP/oled_spi_V0.2.*     # OLED SPI 驱动
   ├─ BSP/w25q64_spi.*        # W25Q64 SPI 驱动
   └─ Project/DDS.uvprojx     # Keil 工程文件
```

---

## 3. 系统原理 | Core Principles

### 3.1 DDS 基本原理 | DDS Fundamentals

### 中文
- FPGA 内部使用相位累加器 + 正弦 ROM（`sine_14bit_256`）实现 DDS。
- 目标频率 `freq_target` 通过比例换算得到频率控制字 `freq_ctrl_word`：
  - 当前实现：`freq_ctrl_word = freq_target * 86`（对应 50 MHz 时钟下的近似系数）。
- 相位高位作为 ROM 地址，输出 14 位波形数据送 DAC。

### 英文
- The FPGA uses a phase accumulator plus sine ROM (`sine_14bit_256`) for DDS generation.
- Target frequency `freq_target` is converted into `freq_ctrl_word`:
  - Current implementation: `freq_ctrl_word = freq_target * 86` (approximation for a 50 MHz clock).
- Upper phase bits index the ROM; the 14-bit sample is sent to the DAC interface.

### 3.2 调制原理 | Modulation Principles

### 中文
- **AM（模式 B）**：载波与包络相乘，包络由调制度 `ma` 与低频调制波生成，并进行限幅输出。
- **FM（模式 C）**：利用调制波对 `fm_fc` 做频偏扰动，动态更新 `phase_step`。
- **ASK/PSK（硬件开关）**：FPGA 生成 10 kbps 基带序列与 100 kHz 载波，完成幅移键控与相移键控输出。
- **李萨如图模式（硬件开关）**：DAC 输出 1 kHz 正弦，同时 `pwm_out` 输出 2 kHz PWM，用于双通道示波观察。
- AM/FM 调制参考波由独立 `mod_phase` 产生（代码中按 1 kHz 设计）。

### English
- **AM (Mode B):** carrier multiplied by an envelope derived from modulation index `ma` and low-frequency modulating wave, then clamped.
- **FM (Mode C):** frequency deviation is applied to `fm_fc`, producing a time-varying `phase_step`.
- **ASK/PSK (hardware switches):** FPGA generates a 10 kbps baseband stream and 100 kHz carrier for digital amplitude/phase keying.
- **Lissajous mode (hardware switch):** DAC outputs a 1 kHz sine while `pwm_out` provides a 2 kHz PWM for dual-channel scope observation.
- The AM/FM modulation reference wave is generated by a separate `mod_phase` path (configured as 1 kHz in code).

### 3.3 通信协议 | MCU–FPGA UART Protocol

### 中文
- 串口波特率：115200。
- 模式字符：
  - `A` → 正弦（Sine）
  - `B` → AM
  - `C` → FM
  - `D` → MCU 界面保留项（当前 ASK/PSK 由 FPGA 拨码开关选择）
- 参数发送格式：ASCII 数字 + `\n`（换行作为提交）。
- FPGA 解析时维护参数序号 `data_idx`，用于 AM/FM 多参数顺序写入。

### English
- UART baud rate: 115200.
- Mode characters:
  - `A` → Sine
  - `B` → AM
  - `C` → FM
  - `D` → Reserved in MCU UI (ASK/PSK are currently selected by FPGA hardware switches)
- Parameter format: ASCII digits + `\n` (newline as commit).
- FPGA tracks `data_idx` to assign multi-parameter inputs for AM/FM in order.

---

## 4. 功能说明 | Features

### 中文
1. **正弦波输出（A）**
   - 支持 Hz/kHz/MHz 量级输入并换算后发送。
2. **AM 调制（B）**
   - 可设置载波频率 `Fc` 与调制度 `ma`。
3. **FM 调制（C）**
   - 可设置载波频率 `Fc` 与频偏 `Dev`（示例 5k/10k 切换）。
4. **ASK/PSK 数字调制（硬件开关）**
   - `sw_ask=1, sw_psk=0`：ASK 输出；`sw_ask=0, sw_psk=1`：PSK 输出。
   - `baseband_out` 输出基带比特流，便于联调观测。
5. **李萨如图测试模式（硬件开关）**
   - `sw_liss=1` 且 ASK/PSK 关闭时，DAC 输出 1 kHz，`pwm_out` 输出 2 kHz PWM。
6. **显示与交互**
   - MCU：OLED 菜单、参数输入与状态反馈。
   - FPGA：6 位数码管显示频率信息与 AM 调制度。
7. **外设支持**
   - 4x4 键盘扫描、OLED SPI、W25Q64 SPI 驱动框架。

### English
1. **Sine output (A)**
   - Accepts Hz/kHz/MHz style input and transmits converted frequency.
2. **AM modulation (B)**
   - Configurable carrier frequency `Fc` and modulation index `ma`.
3. **FM modulation (C)**
   - Configurable carrier `Fc` and deviation `Dev` (e.g., 5k/10k toggle).
4. **ASK/PSK digital modulation (hardware switches)**
   - `sw_ask=1, sw_psk=0`: ASK output; `sw_ask=0, sw_psk=1`: PSK output.
   - `baseband_out` provides the bitstream for debugging/observation.
5. **Lissajous test mode (hardware switch)**
   - With `sw_liss=1` and ASK/PSK off, DAC outputs 1 kHz and `pwm_out` outputs 2 kHz PWM.
6. **Display & interaction**
   - MCU: OLED menu, parameter input, status feedback.
   - FPGA: 6-digit seven-segment display for frequency and AM index.
7. **Peripheral support**
   - 4x4 keypad scanning, OLED SPI driver, W25Q64 SPI framework.

---

## 5. 软硬件环境 | Requirements

### 中文
- FPGA 开发：Intel Quartus Prime 18.0（工程文件已匹配）。
- MCU 开发：Keil uVision + TI MSPM0 SDK（`mspm0_sdk@2.08.00.03`）。
- 目标器件：
  - FPGA：Cyclone IV E（`EP4CE6F17C8`）
  - MCU：MSPM0G3519
- 调试下载：J-Link（Keil 工程内含相关配置）。

### English
- FPGA toolchain: Intel Quartus Prime 18.0 (project already configured).
- MCU toolchain: Keil uVision + TI MSPM0 SDK (`mspm0_sdk@2.08.00.03`).
- Target devices:
  - FPGA: Cyclone IV E (`EP4CE6F17C8`)
  - MCU: MSPM0G3519
- Debug/programming: J-Link (config present in the Keil project).

---

## 6. 使用方法 | How to Use

### 6.1 FPGA 端 | FPGA Side

### 中文
1. 用 Quartus 打开 `DDS_FPGA/DDS_simple.qpf`。
2. 检查 `DDS_simple.qsf` 中引脚是否与硬件一致（时钟、UART、DAC、数码管、开关）。
3. 编译并下载到 FPGA。
4. 按需设置开关模式：
   - `sw_ask=1, sw_psk=0`：ASK
   - `sw_ask=0, sw_psk=1`：PSK
   - `sw_liss=1` 且其余关闭：李萨如模式（DAC 1 kHz + PWM 2 kHz）
5. 确认 DAC 时钟 PLL 已锁定（`locked` 信号）。

### English
1. Open `DDS_FPGA/DDS_simple.qpf` in Quartus.
2. Verify `DDS_simple.qsf` pin assignments match your board (clock, UART, DAC, display, switch).
3. Compile and program the FPGA.
4. Set switch mode as needed:
   - `sw_ask=1, sw_psk=0`: ASK
   - `sw_ask=0, sw_psk=1`: PSK
   - `sw_liss=1` with others off: Lissajous mode (DAC 1 kHz + PWM 2 kHz)
5. Confirm DAC PLL lock (`locked` signal).

### 6.2 MCU 端 | MCU Side

### 中文
1. 用 Keil 打开 `DDS_MCU/Project/DDS.uvprojx`。
2. 确保 SysConfig 生成步骤可用（工程中配置了构建前调用 `syscfg.bat`）。
3. 编译并下载到 MSPM0G3519。
4. 上电后通过键盘进入模式 A/B/C，输入参数并发送。

### English
1. Open `DDS_MCU/Project/DDS.uvprojx` in Keil.
2. Ensure SysConfig pre-build generation works (`syscfg.bat` is configured in project settings).
3. Build and flash the MSPM0G3519 firmware.
4. After boot, use keypad to select A/B/C modes, input parameters, and transmit.

### 6.3 联调流程 | End-to-End Bring-Up

### 中文
1. 先下载 FPGA，再下载 MCU。
2. 确认 MCU UART TX 与 FPGA `uart_rx` 连线正确且共地。
3. 在 MCU 上选择模式并发送参数，观察：
   - FPGA DAC 输出波形
   - FPGA 数码管显示
   - MCU OLED 反馈

### English
1. Program FPGA first, then MCU.
2. Verify MCU UART TX ↔ FPGA `uart_rx` wiring and common ground.
3. Select mode and send parameters from MCU; observe:
   - FPGA DAC waveform output
   - FPGA seven-segment display
   - MCU OLED feedback

---

## 7. 当前实现注意事项 | Current Implementation Notes

### 中文
- 代码中保留了较多历史注释与备份文件（`.bak`、注释掉的旧逻辑），调试时需注意当前生效段落。
- ASK/PSK 与李萨如模式由 FPGA 侧硬件开关直接控制，与 MCU 串口模式选择（A/B/C）并行存在。
- 仓库包含大量 `Source/third_party` 内容（SDK/第三方库示例），核心业务逻辑主要在 `DDS_FPGA/` 与 `DDS_MCU/User`、`DDS_MCU/BSP`。

### English
- The codebase contains many historical comments/backups (`.bak`, commented legacy code); make sure you modify active logic blocks.
- ASK/PSK and Lissajous modes are controlled directly by FPGA hardware switches and coexist with MCU UART A/B/C mode control.
- The repository includes large `Source/third_party` content (SDK/examples); core project logic is primarily in `DDS_FPGA/` plus `DDS_MCU/User` and `DDS_MCU/BSP`.

---

## 8. 最近更新要点 | Recent Update Highlights

### 中文
1. **Sine 模式发送链路修复**
   - 模式 A 下支持先输入数字再选择单位（A=Hz / B=kHz / C=MHz）后发送到 FPGA。
   - 使用 `*` / `#` 做 100 Hz 步进微调时，会自动按 Hz 显示并立即发送最新频率。
2. **AM 参数下发与显示修复**
   - 进入模式 B 时，MCU 默认参数为 `Fc=1.0 MHz`、`ma=10%`，并立即同步到 FPGA。
   - MCU 侧通过“模式字符 + 双参数”顺序下发（`B` + `Fc` + `ma`），避免参数索引错位。
3. **FM 参数下发与显示修复**
   - 进入模式 C 时，默认 `Fc=100 kHz`、`Dev=5 kHz`，并立即同步到 FPGA。
   - `*` 键可在 `5 kHz / 10 kHz` 频偏间切换并实时下发。
4. **FPGA 显示与李萨如模式修复**
   - FM 模式下数码管显示区域会显示 `fm_dev / 1000`（kHz 量级），便于直接观察频偏设置。
   - 李萨如模式新增独立 1 kHz / 2 kHz 相位累加路径，保证 DAC 与 PWM 双通道输出稳定。

### English
1. **Sine mode transmission path fixed**
   - In Mode A, users can input digits first, then choose unit (A=Hz / B=kHz / C=MHz) before transmission.
   - Fine tuning with `*` / `#` (100 Hz step) now switches display to Hz and sends the latest frequency immediately.
2. **AM parameter transfer and display fixed**
   - Entering Mode B initializes MCU defaults to `Fc=1.0 MHz`, `ma=10%`, and syncs them to FPGA immediately.
   - MCU now sends AM as an ordered sequence (`B` + `Fc` + `ma`) to keep FPGA parameter indexing aligned.
3. **FM parameter transfer and display fixed**
   - Entering Mode C initializes `Fc=100 kHz`, `Dev=5 kHz`, then synchronizes immediately.
   - `*` toggles deviation between `5 kHz` and `10 kHz` and pushes the update in real time.
4. **FPGA display and Lissajous mode fixed**
   - In FM mode, seven-segment display now shows `fm_dev / 1000` (kHz-scale) for direct deviation observation.
   - Lissajous mode uses dedicated 1 kHz / 2 kHz phase accumulators for stable DAC + PWM dual-channel output.

---

## 9. 后续优化建议 | Future Recommendations

### 中文
1. **统一模式控制入口**：将硬件开关模式与 MCU 菜单模式进行统一管理，避免状态冲突。  
2. **协议增强**：加入帧头、长度、校验（CRC）与 ACK，提高抗干扰能力。  
3. **参数边界保护**：在 MCU 与 FPGA 双侧统一做频率、调制度、频偏范围限制。  
4. **数值精度优化**：将 `freq_target * 86` 升级为更高精度定点换算。  
5. **工程清理**：分离生成文件、备份文件与源文件，减少仓库噪音。  
6. **自动化验证**：增加仿真 testbench（UART 输入序列、AM/FM 频谱与幅度检查）。  
7. **文档与硬件图**：补充接线图、串口指令手册、模式时序图。  

### English
1. **Unify mode control path** between hardware switches and MCU menu to avoid conflicting states.  
2. **Strengthen UART protocol** with frame header, length, CRC, and ACK.  
3. **Add consistent parameter limits** on both MCU and FPGA sides.  
4. **Improve frequency-word precision** beyond the current `* 86` approximation.  
5. **Clean repository artifacts** (generated outputs/backups vs source files).  
6. **Add automated verification** (testbench for UART sequences and AM/FM checks).  
7. **Expand documentation** with wiring diagrams, command reference, and timing charts.  

---

## 10. 许可与说明 | License & Notes

### 中文
- 仓库中部分第三方目录和生成文件包含原始版权声明，请按各自许可证使用。  

### English
- Some third-party/generated directories include their own copyright and license notices; follow those respective licenses.

## 贡献
<a href="https://github.com/shantu246/DDS/graphs/contributors">
  <img src="https://contrib.rocks/image?repo=shantu246/DDS" />
</a>
